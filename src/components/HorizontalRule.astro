<hr class="separator" />

<style>
  hr {
    /* Without this, hr disappears when in a flex or grid container */
    margin: 0.01rem 0;
    border: none;
    height: 3px;
    background: var(--color-2);
    background: -moz-linear-gradient(
      90deg,
      var(--color-2) 0%,
      var(--color-3) 100%
    );
    background: -webkit-linear-gradient(
      90deg,
      var(--color-2) 0%,
      var(--color-3) 100%
    );
    background: linear-gradient(90deg, var(--color-2) 0%, var(--color-3) 100%);
    filter: progid:DXImageTransform.Microsoft.gradient(startColorstr="#b40002", endColorstr="#0057b7", GradientType=1);
  }
</style>
