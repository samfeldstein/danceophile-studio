---
import Logo from "./Logo.astro";
import MobileMenu from "./MobileMenu.astro";
---

<header>
  <Logo class="logo" />
</header>
<!-- This is outside the header because if it's inside sticky stops working. -->
<MobileMenu />

<style>
  header {
    display: flex;
    flex-direction: column;
    margin-bottom: 2rem;
  }

  .logo {
    font-size: calc(2rem + 4vw);
  }
</style>
